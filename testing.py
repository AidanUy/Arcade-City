import sys
import pygame
import screenclass 



def main():
	from screenclass import Screen
	a_screen = Screen()
	a_screen.background1
	#print(home_screen.imagereference)
	#background = pygame.image.load("imagetest.jpg").convert()
	#screen.blit(background, [0,0])
	#home_screen.imagereference = pygame.image.load("imagetest.jpg").convert()
	
	#trying to see if blit would work on this test file, BUT IT WILL NOT. not self.screen.blit on the main file will not work
	#either, because screen is defined in the class file.
	
	#EVERYTHING IN THE HASHTAG ARE FAILED ATTEMPTS!!!!!!!!!!!
main()
